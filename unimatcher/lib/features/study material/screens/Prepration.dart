import 'package:flutter/material.dart';

class PreprationScreen extends StatelessWidget {
  const PreprationScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
